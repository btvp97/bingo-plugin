# Metadata used when this plugin is submitted to the Plugin Hub.
# See: https://github.com/runelite/plugin-hub
displayName=Clan Bingo
author=Great Marion
description=Coordinate clan bingo boards with live, join-code-protected team sync
tags=bingo,pvm,minigame,clan,skilling
plugins=com.misclickers.bingo.BingoPlugin
version=
# "gradle" (not "standard") is required here because we depend on
# io.socket:socket.io-client for live team sync, which isn't already part of
# runelite-client's own dependencies. "standard" mode would silently replace
# our build.gradle/settings.gradle during the Hub build and drop it. See
# plugin-hub's README "Third party dependencies" section for what this means
# for review time.
build=gradle
